go to jenkin.war directory
open cmd
run " java -jar jenkins.war."
open ngrok app (in downloads)
run "ngrok http 8080"
now the ip lcoalhost:8080 will be directed to a public ip, use that


building the dockerfile
"make build" ---------->building the image
"make run"   ---------->running the image
"make clean" ---------->remove the image